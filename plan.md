# Plan

Feature: Windows executable launcher selects a download folder and writes
results to `download/korus_yyyymm`.

Tests (TDD order)
- [x] TEST-PLAN-1: `make_korus_save_dir` creates `korus_yyyymm` under the
      selected download directory when given a fixed month.
- [x] TEST-PLAN-2: `run_with_download_dir` uses `make_korus_save_dir` and
      passes the derived save directory to the checker/zip workflow.
- [x] TEST-PLAN-3A: `launcher.main` calls `run_with_download_dir` when a
      folder is selected.
- [x] TEST-PLAN-3B: `launcher.main` exits when the folder selection is canceled.
- [x] TEST-PLAN-4: `scripts/build_windows.ps1` contains the PyInstaller
      arguments for `src.launcher` and checker submodules.
- [x] TEST-PLAN-5: `pyproject.toml` dev dependencies include PyInstaller.
- [x] TEST-PLAN-6: README documents Windows build and run steps for the
      launcher executable.

Feature: GitHub Actions CI and Release

Tests (TDD order)
- [x] TEST-PLAN-7: `ci.yml` runs ruff, mypy, pytest on push/PR.
- [x] TEST-PLAN-8: `release.yml` builds Windows exe and attaches it to a
      release whose tag matches `pyproject.toml` version.
- [x] TEST-PLAN-9: `ci.yml` runs `ruff format --check`.
- [x] TEST-PLAN-10: `release.yml` creates a git tag when missing.
- [x] TEST-PLAN-11: `release.yml` enables autogenerated release notes.
